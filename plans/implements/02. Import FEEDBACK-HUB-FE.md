# Import FEEDBACK-HUB-FE into `frontend/`

## Context

The frontend app already exists as its own repo at `/home/daont/workspaces/personal-projects/FEEDBACK-HUB-FE` (Vue 3 + Vite, TypeScript, a generic "app-starter" stripped down from an older SEHO chat client — same lineage as the backend's own "stripped to generic template" history in `.claude/decisions.md`). Now that the backend repo has been restructured into `backend/`/`frontend/`/`shared/`, this step brings the FE app's working tree into `frontend/`.

Per your answers when this was scoped:
- **No git history import** — plain copy, single fresh commit in the monorepo (not `git subtree`).
- **No legacy docs carried over** — moot here: this source repo has no `documents/`/`plans/`/`docs/` at all (already clean).
- **Drop FE's nested tool config** (`.claude/`, `.serena/`, `.github/`, `CLAUDE.md`, `AGENTS.md`) — the monorepo root config governs everything now. Non-tool-config content worth keeping (`README.md`, `CHANGELOG.md`, `Jenkinsfile`) moves into `frontend/`, adapted to FeedbackHub instead of the old SEHO chat-bot project.

Source repo state: `git status` clean, `main` up to date with `origin/main`, HEAD `9fd8dc5` — no divergence to worry about (this supersedes the earlier messier copy I inspected under `frontends/FEEDBACK-HUB-FE`, which no longer exists).

## What moves, what doesn't

**Copy as-is** (source, tests, build config — all paths are relative, unaffected by the new parent directory):
`src/`, `tests/`, `e2e/`, `public/`, `index.html`, `env.d.ts`, `tsconfig*.json` (5 files), `vite.config.ts`, `vitest.config.ts`, `playwright.config.ts`, `.prettierrc.json`, `bun.lock`, `.vscode/extensions.json`, `.env.e2e.example` (already generic, no chat-specific content).

**Copy + adapt:**
- `package.json` — rename `"name": "app-starter"` → `"feedback-hub-frontend"`; leave dependencies/scripts untouched.
- `.env.example` — `VITE_API_BASE_URL` currently `http://localhost:5000` (stale port); backend's actual dev URL is `http://localhost:7000` (`backend/src/FeedbackHub.API/Properties/launchSettings.json`) and controllers route under `api/[controller]` with no `/api` prefix added in code (`src/api/client.ts` uses `VITE_API_BASE_URL` as the literal axios `baseURL`) → set to `http://localhost:7000/api`. `BASE_URL=/` is already correct, no change.
- `.gitignore` → copied as `frontend/.gitignore` (nested, git supports this natively) — unchanged content, already covers `node_modules`/`dist`/`.env`/etc.
- `README.md` → merge into the existing `frontend/README.md` placeholder (currently just a stack note) rather than overwrite with FE's near-empty `# App Starter`.
- `CHANGELOG.md` → copy to `frontend/CHANGELOG.md` as-is — content already describes this app-starter, nothing SEHO-specific.
- `Jenkinsfile` → copy to `frontend/Jenkinsfile`, but its `DEPLOY_PATH`, `VITE_API_BASE_URL`/`BASE_URL` env-file stage, and target host still point at the old SEHO chat-bot deploy (`/data/wpe-tools/WPE-CHATBOT/bot`, `dev-tools.seho.vn/api/chat-bot`). I don't have the real FeedbackHub deploy target, so I'll replace those values with clearly-marked `TODO` placeholders rather than guess — **flagging this as a manual follow-up you'll need to fill in before this pipeline is usable.**

**Dropped (not copied — duplicates root monorepo config):**
`.claude/`, `.serena/`, `.github/`, `CLAUDE.md`, `AGENTS.md`, `.git/` (no history import, per your answer).

**Not touched:** the source repo at `/home/daont/workspaces/personal-projects/FEEDBACK-HUB-FE` is left exactly as-is (this is a copy, not a move) — its own `.git`/remote stay intact for you to archive or delete later, out of scope here.

## Steps

1. `rsync -a` copy from the source repo into `frontend/`, excluding `.git/`, `.claude/`, `.serena/`, `.github/`, `CLAUDE.md`, `AGENTS.md`, `node_modules/` (not present but exclude defensively), and the files needing hand-edits (`package.json`, `.env.example`, `README.md`, `CHANGELOG.md`, `Jenkinsfile`, `.gitignore`) which get written explicitly in step 2.
2. Write the adapted versions of `package.json`, `.env.example`, `frontend/.gitignore`, merged `README.md`, `CHANGELOG.md`, `Jenkinsfile` (with TODO deploy placeholders) as described above.
3. `bun install` inside `frontend/`, then `bun run type-check` and `bun run test:run` (vitest, no browser needed) as a smoke test that the copied app is self-contained in its new location. Skip `test:e2e` (needs Playwright browser install + a running backend — out of scope for this move).
4. Log the decision in `.claude/decisions.md` (prepend): FE imported as a plain copy (no git history), nested tool config dropped in favor of root, Jenkinsfile deploy target left as TODO.
5. Re-run GitNexus (`npx gitnexus analyze --force`) so the unified `FEEDBACK-HUB` index picks up the new `frontend/` source tree, then `gitnexus_detect_changes(scope: "all", repo: "FEEDBACK-HUB")` to confirm the diff is scoped to the new files with no unexpected impact.
6. `git status` review — stop here, same as the backend move: no commit until you review and say go.

**Not doing:** no git history import, no CI wiring beyond copying the existing (soon-to-be-fixed) `Jenkinsfile`, no fixing the Jenkinsfile deploy target myself (need real values from you), no touching the source `FEEDBACK-HUB-FE` repo.

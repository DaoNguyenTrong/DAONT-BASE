# Notification Module — Phase 1 (In-App + Polling)

## Context

Thiết kế tổng thể đã chốt tại `plans/v1/notification-module-architecture.md` (3 phase: in-app+polling → multi-channel qua Hangfire → push cho mobile/offline). Plan này chỉ triển khai **Phase 1**: bảng `Notification` + REST + frontend poll. **Không** làm trong phase này: `INotificationChannel`, `IBackgroundJobDispatcher`, Hangfire fan-out, `EmailNotificationChannel` — đó là Phase 2, chưa cần thiết vì chưa có kênh thứ 2 nào để trừu tượng hoá.

**Ground truth tại thời điểm viết plan** (working tree đang có thay đổi chưa commit — plan #13 "Clean Architecture Cleanup", chưa merge): `AppException` là base exception hiện tại (không còn `ApiException`/`StatusCode`/`Title` trên Domain — status code map ở `AppExceptionHttpMapper` phía API), `OrganizationService` đã tách `OrganizationMembershipService` riêng cho `AddMemberAsync`/`UpdateMemberRolesAsync`/`RemoveMemberAsync`. Notification module này build trên nền post-refactor đó, không trên state trước đó.

**Tiền đề bắt buộc**: Phase 1 **phụ thuộc #13 đã merge vào `dev`** trước khi bắt đầu — `OrganizationMembershipService.cs` và `AppException` chỉ tồn tại trong working tree chưa commit hiện tại. Nếu #13 chưa merge, bước "sửa `OrganizationMembershipService`" ở Part 3 nhắm vào một file chưa tồn tại trên `dev`, và các exception dùng trong plan này (`NotFoundException`, `UnauthorizedException` kế thừa `AppException`) sẽ sai nếu build trên state trước #13.

**Thay đổi so với sketch ban đầu (quan trọng)**: bỏ `Title`/`Body` khỏi `Notification`/`NotificationParams`. Lý do: backend hiện **không có** khái niệm locale-per-account (bảng `Account` không có cột locale/ngôn ngữ), nên không có cách nào chọn đúng ngôn ngữ để render sẵn Title/Body lúc tạo notification. Theo đúng cross-cutting rule của `localization.md` ("ưu tiên để frontend map theo key thay vì render chuỗi backend trực tiếp"), Phase 1 chỉ lưu `Type` (string, key ổn định) + `Data` (jsonb, tham số interpolation) — **frontend tự render** Title/Body từ `locales/{vi,en}.ts` theo `Type`. Điều này cũng giải quyết luôn mục Open Question "Localization" trong tài liệu kiến trúc, không cần resx riêng cho notification content.

**Use case chứng minh (proof-of-concept caller)**: `OrganizationMembershipService.AddMemberAsync` — sau khi thêm member vào org thành công, gọi `NotifyAsync` báo cho account vừa được thêm. Chọn đây làm ví dụ tham chiếu vì đã tồn tại sẵn, có recipient rõ ràng, giống cách plan #12 dùng `RefreshTokenCleanupJob` làm ví dụ tham chiếu khi wiring Hangfire.

---

## Part 1 — Domain

### New: `backend/src/StarterKit.Domain/Entities/Notification.cs`

Theo mẫu `RefreshToken`/`EmailVerificationToken` — Create-only, không có `Update` chung, chỉ có mutator nhỏ:

```csharp
using StarterKit.Domain.Exceptions;

namespace StarterKit.Domain.Entities;

public record NotificationParams(Guid AccountId, string Type, string? Data = null);

public sealed class Notification : BaseEntity<Guid>
{
    private Notification() { }

    public static Notification Create(NotificationParams p)
    {
        if (p.AccountId == Guid.Empty)
            throw new DomainException(DomainMessages.AccountIdRequired);

        if (string.IsNullOrWhiteSpace(p.Type))
            throw new DomainException(DomainMessages.NotificationTypeRequired);

        return new Notification
        {
            Id = IdGenerator.NewUuidV7(),
            AccountId = p.AccountId,
            Type = p.Type.Trim(),
            Data = p.Data
        };
    }

    public Guid AccountId { get; private set; }
    public string Type { get; private set; } = string.Empty;
    public string? Data { get; private set; }
    public DateTime? ReadAt { get; private set; }
    public bool IsRead => ReadAt.HasValue;

    public void MarkRead()
    {
        if (!ReadAt.HasValue) ReadAt = DateTime.UtcNow;
    }
}
```

### Edit: `backend/src/StarterKit.Domain/Exceptions/DomainMessages.cs`

Thêm 1 hằng số (mẫu `nameof`, giống mọi entry khác trong file):

```csharp
public const string NotificationTypeRequired = nameof(NotificationTypeRequired);
```

(`AccountIdRequired` đã có sẵn, dùng lại — không thêm mới.)

---

## Part 2 — Infrastructure

### New: `backend/src/StarterKit.Infrastructure/Persistence/Configurations/NotificationConfiguration.cs`

Theo mẫu `RefreshTokenConfiguration.cs` — FK cascade tới `Account`, index theo `AccountId` (mọi query đều lọc theo account) và composite `(AccountId, ReadAt)` (phục vụ trực tiếp query đếm unread — xem đánh giá tải ở `plans/v1/notification-module-architecture.md` mục 7):

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using StarterKit.Domain.Entities;

namespace StarterKit.Infrastructure.Persistence.Configurations;

public sealed class NotificationConfiguration : IEntityTypeConfiguration<Notification>
{
    public void Configure(EntityTypeBuilder<Notification> builder)
    {
        builder.ToTable("notifications");

        builder.HasKey(n => n.Id);

        builder.Property(n => n.Type)
            .HasMaxLength(100)
            .IsRequired();

        builder.Property(n => n.Data)
            .HasColumnType("jsonb");

        builder.Property(n => n.CreatedAt)
            .IsRequired();

        builder.HasIndex(n => n.AccountId);

        builder.HasIndex(n => new { n.AccountId, n.ReadAt });

        builder.HasOne<Account>()
            .WithMany()
            .HasForeignKey(n => n.AccountId)
            .OnDelete(DeleteBehavior.Cascade);
    }
}
```

### Edit: `backend/src/StarterKit.Infrastructure/Persistence/AppDbContext.cs`

Thêm cạnh các `DbSet` khác (thứ tự alphabet theo convention hiện có):

```csharp
public DbSet<Notification> Notifications => Set<Notification>();
```

### Migration

```bash
dotnet ef migrations add AddNotifications --project backend/src/StarterKit.Infrastructure --startup-project backend/src/StarterKit.API
```

Không cần seed data.

---

## Part 3 — Application

### New: `backend/src/StarterKit.Application/Services/Notifications/NotificationDto.cs`

```csharp
namespace StarterKit.Application.Services.Notifications;

public sealed record NotificationDto(Guid Id, string Type, string? Data, bool IsRead, DateTime CreatedAt);
```

### New: `backend/src/StarterKit.Application/Services/Notifications/UnreadCountDto.cs`

Endpoint đếm tách riêng khỏi list (theo đánh giá tải — badge không cần fetch cả trang list):

```csharp
namespace StarterKit.Application.Services.Notifications;

public sealed record UnreadCountDto(int Count);
```

### New: `backend/src/StarterKit.Application/Services/Notifications/NotificationTypes.cs`

Catalog cố định, giống cách `Permissions.All` là catalog code-defined (không cho user tự tạo `Type` tuỳ ý):

```csharp
namespace StarterKit.Application.Services.Notifications;

public static class NotificationTypes
{
    public const string OrganizationMemberAdded = nameof(OrganizationMemberAdded);
}
```

### Edit: `backend/src/StarterKit.Application/Common/Mappings/EntityMapper.cs`

Thêm partial method — `NotificationDto` không có `AccountId`/`ReadAt`/`UpdatedAt`/`CreatedBy`/`UpdatedBy`, cần `[MapperIgnoreSource]` cho cả 5 (giống `ToDto(Account)` đã làm với `CreatedBy`/`UpdatedBy`/`PasswordHash`) — thiếu sẽ ăn cảnh báo Mapperly RMG020, vi phạm ngưỡng 0-warning của `dotnet build`:

```csharp
[MapperIgnoreSource(nameof(Notification.AccountId))]
[MapperIgnoreSource(nameof(Notification.ReadAt))]
[MapperIgnoreSource(nameof(Notification.UpdatedAt))]
[MapperIgnoreSource(nameof(Notification.CreatedBy))]
[MapperIgnoreSource(nameof(Notification.UpdatedBy))]
public static partial NotificationDto ToDto(Notification notification);
```

### New: `backend/src/StarterKit.Application/Services/Notifications/INotificationService.cs`

```csharp
using StarterKit.Application.Common.Models;
using StarterKit.Domain.Entities;

namespace StarterKit.Application.Services.Notifications;

public interface INotificationService
{
    Task NotifyAsync(NotificationParams request, CancellationToken cancellationToken);

    Task<PagedResult<NotificationDto>> GetMyNotificationsAsync(
        PaginationRequest request, bool? unreadOnly, CancellationToken cancellationToken);

    Task<int> GetUnreadCountAsync(CancellationToken cancellationToken);

    Task MarkAsReadAsync(Guid notificationId, CancellationToken cancellationToken);

    Task MarkAllAsReadAsync(CancellationToken cancellationToken);
}
```

### New: `backend/src/StarterKit.Application/Services/Notifications/NotificationService.cs`

`NotifyAsync` **chỉ persist** ở Phase 1 — không enqueue job, không channel (đó là Phase 2). 5 method, 2 dependency — dưới ngưỡng God Class đã áp dụng ở plan #13.

```csharp
using System.Linq.Expressions;
using StarterKit.Application.Common.Interfaces;
using StarterKit.Application.Common.Models;
using StarterKit.Application.Resources;
using StarterKit.Domain.Entities;
using StarterKit.Domain.Exceptions;
using StarterKit.Domain.Interfaces;

namespace StarterKit.Application.Services.Notifications;

public sealed class NotificationService(
    IUnitOfWork unitOfWork,
    ICurrentUserService currentUserService) : INotificationService
{
    private const int DefaultPageNumber = 1;
    private const int DefaultPageSize = 10;

    public async Task NotifyAsync(NotificationParams request, CancellationToken cancellationToken)
    {
        Notification notification = Notification.Create(request);
        await unitOfWork.Repository<Notification, Guid>().AddAsync(notification, cancellationToken);
        await unitOfWork.SaveChangesAsync(cancellationToken);
    }

    public async Task<PagedResult<NotificationDto>> GetMyNotificationsAsync(
        PaginationRequest request, bool? unreadOnly, CancellationToken cancellationToken)
    {
        Guid accountId = GetCurrentAccountId();
        int pageNumber = request.PageNumber < 1 ? DefaultPageNumber : request.PageNumber;
        int pageSize = request.PageSize < 1 ? DefaultPageSize : request.PageSize;

        Expression<Func<Notification, bool>> predicate = unreadOnly == true
            ? n => n.AccountId == accountId && n.ReadAt == null
            : n => n.AccountId == accountId;

        (IReadOnlyList<Notification> items, int totalCount) = await unitOfWork
            .Repository<Notification, Guid>()
            .ListPagedAsync(predicate, pageNumber, pageSize, cancellationToken);

        return new PagedResult<NotificationDto>(
            items.Select(EntityMapper.ToDto).ToList(), totalCount, pageNumber, pageSize);
    }

    public async Task<int> GetUnreadCountAsync(CancellationToken cancellationToken)
    {
        Guid accountId = GetCurrentAccountId();

        // Không thêm CountAsync riêng vào IRepository (blast radius rộng, ảnh hưởng mọi repository) —
        // ListPagedAsync với pageSize=1 vẫn chỉ chạy 1 câu COUNT thật sự rẻ, chỉ dư 1 row SELECT không dùng.
        (_, int totalCount) = await unitOfWork.Repository<Notification, Guid>()
            .ListPagedAsync(n => n.AccountId == accountId && n.ReadAt == null, 1, 1, cancellationToken);

        return totalCount;
    }

    public async Task MarkAsReadAsync(Guid notificationId, CancellationToken cancellationToken)
    {
        Guid accountId = GetCurrentAccountId();
        IRepository<Notification, Guid> repository = unitOfWork.Repository<Notification, Guid>();

        Notification notification = await repository.GetByIdAsync(notificationId, cancellationToken)
            ?? throw new NotFoundException(nameof(Notification), notificationId);

        if (notification.AccountId != accountId)
        {
            // 404, không phải 403 — không tiết lộ sự tồn tại của notification thuộc account khác.
            throw new NotFoundException(nameof(Notification), notificationId);
        }

        notification.MarkRead();
        repository.Update(notification);
        await unitOfWork.SaveChangesAsync(cancellationToken);
    }

    public async Task MarkAllAsReadAsync(CancellationToken cancellationToken)
    {
        Guid accountId = GetCurrentAccountId();
        IRepository<Notification, Guid> repository = unitOfWork.Repository<Notification, Guid>();

        IReadOnlyList<Notification> unread = await repository.ListAsync(
            n => n.AccountId == accountId && n.ReadAt == null, cancellationToken);

        foreach (Notification notification in unread)
        {
            notification.MarkRead();
            repository.Update(notification);
        }

        await unitOfWork.SaveChangesAsync(cancellationToken);
    }

    private Guid GetCurrentAccountId()
    {
        if (!Guid.TryParse(currentUserService.UserId, out Guid accountId))
        {
            throw new UnauthorizedException(ApplicationMessages.AuthenticatedUserRequired);
        }

        return accountId;
    }
}
```

`MarkAllAsReadAsync` load hết unread vào memory rồi update từng dòng — chấp nhận được ở quy mô hiện tại; nếu sau này 1 account có thể có hàng nghìn unread, cân nhắc `ExecuteUpdateAsync` (EF Core bulk update) thay vì loop.

### Edit: `backend/src/StarterKit.Application/DependencyInjection.cs`

```csharp
services.AddScoped<INotificationService, NotificationService>();
```

### Edit: `backend/src/StarterKit.Application/Services/Organizations/OrganizationMembershipService.cs`

Thêm `INotificationService` vào constructor, gọi `NotifyAsync` sau khi `SaveChangesAsync` + invalidate cache trong `AddMemberAsync` (không đổi `UpdateMemberRolesAsync`/`RemoveMemberAsync`):

```csharp
public sealed class OrganizationMembershipService(
    IUnitOfWork unitOfWork,
    ITenantAccessService tenantAccessService,
    IPermissionResolver permissionResolver,
    INotificationService notificationService) : IOrganizationMembershipService
{
    public async Task AddMemberAsync(Guid organizationId, AddMemberRequest request, CancellationToken cancellationToken)
    {
        // ... toàn bộ thân hàm hiện tại giữ nguyên tới hết 3 dòng invalidate cache, thêm ngay sau:

        await notificationService.NotifyAsync(
            new NotificationParams(account.Id, NotificationTypes.OrganizationMemberAdded,
                JsonSerializer.Serialize(new { organizationId })),
            cancellationToken);
    }
    // ...
}
```

Phase 1 chỉ nhúng `organizationId` vào `Data` (không kèm tên tổ chức) — tránh phải load thêm `Organization` entity chỉ để lấy tên trong write path; frontend tự tra tên tổ chức nếu cần khi render (đã có `organizationId` để deep-link/gọi API tổ chức nếu muốn hiển thị tên).

### Edit: `backend/tests/StarterKit.Application.Tests/Services/Organizations/OrganizationMembershipServiceTests.cs`

File này **đã tồn tại** (từ plan #13) và `CreateFixture()` dựng `OrganizationMembershipService` bằng `new(unitOfWork, tenantAccessService, permissionResolver)` — thêm tham số thứ 4 vào constructor thật sẽ làm file này fail biên dịch nếu không sửa theo. Cập nhật `Fixture` record + `CreateFixture()`:

```csharp
private sealed record Fixture(
    OrganizationMembershipService Service,
    IRepository<OrganizationMember, Guid> MemberRepo,
    IRepository<OrganizationMemberRole, Guid> MemberRoleRepo,
    IRepository<Role, Guid> RoleRepo,
    IRepository<Account, Guid> AccountRepo,
    IUnitOfWork UnitOfWork,
    ITenantAccessService TenantAccessService,
    IPermissionResolver PermissionResolver,
    INotificationService NotificationService); // thêm

private static Fixture CreateFixture()
{
    // ... giữ nguyên phần dựng các repo/substitute hiện có ...

    INotificationService notificationService = Substitute.For<INotificationService>();

    OrganizationMembershipService service = new(
        unitOfWork, tenantAccessService, permissionResolver, notificationService);

    return new Fixture(
        service, memberRepo, memberRoleRepo, roleRepo, accountRepo,
        unitOfWork, tenantAccessService, permissionResolver, notificationService);
}
```

Không cần assert `NotificationService.Received().NotifyAsync(...)` trong các test hiện có của `AddMemberAsync*` trừ khi muốn — substitute mặc định no-op là đủ để các test đang có tiếp tục pass nguyên trạng. Có thể thêm 1 test mới `AddMemberAsync_NewMember_SendsNotification` để phủ đường gọi `NotifyAsync`.

---

## Part 4 — API

### New: `backend/src/StarterKit.API/Controllers/NotificationsController.cs`

Route lowercase theo `api-contract.md`, mọi action `[Authorize]` (mặc định — không cần `[AllowAnonymous]`):

```csharp
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using StarterKit.Application.Common.Models;
using StarterKit.Application.Services.Notifications;

namespace StarterKit.API.Controllers;

[ApiController]
[Authorize]
[Route("api/notifications")]
public sealed class NotificationsController(INotificationService notificationService) : ControllerBase
{
    /// <summary>Returns a paginated list of the current account's notifications, newest first.</summary>
    [HttpGet]
    [ProducesResponseType(typeof(PagedResult<NotificationDto>), StatusCodes.Status200OK)]
    public async Task<ActionResult<PagedResult<NotificationDto>>> GetAll(
        [FromQuery] PaginationRequest request,
        [FromQuery] bool? unreadOnly,
        CancellationToken cancellationToken)
    {
        return Ok(await notificationService.GetMyNotificationsAsync(request, unreadOnly, cancellationToken));
    }

    /// <summary>Returns the unread notification count for the current account.</summary>
    [HttpGet("unread-count")]
    [ProducesResponseType(typeof(UnreadCountDto), StatusCodes.Status200OK)]
    public async Task<ActionResult<UnreadCountDto>> GetUnreadCount(CancellationToken cancellationToken)
    {
        return Ok(new UnreadCountDto(await notificationService.GetUnreadCountAsync(cancellationToken)));
    }

    /// <summary>Marks a single notification as read. 404 if it doesn't belong to the current account.</summary>
    [HttpPatch("{id:guid}/read")]
    [ProducesResponseType(StatusCodes.Status204NoContent)]
    [ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status404NotFound)]
    public async Task<IActionResult> MarkAsRead(Guid id, CancellationToken cancellationToken)
    {
        await notificationService.MarkAsReadAsync(id, cancellationToken);
        return NoContent();
    }

    /// <summary>Marks every unread notification for the current account as read.</summary>
    [HttpPatch("read-all")]
    [ProducesResponseType(StatusCodes.Status204NoContent)]
    public async Task<IActionResult> MarkAllAsRead(CancellationToken cancellationToken)
    {
        await notificationService.MarkAllAsReadAsync(cancellationToken);
        return NoContent();
    }
}
```

### OpenAPI

```bash
dotnet build backend/src/StarterKit.API/StarterKit.API.csproj --no-restore -m:1 -p:OpenApiGenerateDocumentsOnBuild=true
```

---

## Part 5 — Frontend

### Codegen

```bash
bun run --cwd frontend codegen
```

Sinh `getNotifications()` factory (`notificationsGetAll`, `notificationsGetUnreadCount`, `notificationsMarkAsRead`, `notificationsMarkAllAsRead`) từ spec mới.

### Edit: `frontend/src/api/types.ts`

```ts
export type { NotificationDto as Notification } from './generated/model/notificationDto'
```

`totalCount`/`pageNumber`/`pageSize` của `PagedResultOfNotificationDto` đi qua `toPagedResult()` sẵn có (`lib/paged-result.ts`) như mọi list khác — không cần code mới.

### New: `frontend/src/stores/notifications.ts`

Setup-store Pinia, poll **có nhận biết visibility** (theo đánh giá UX ở `plans/v1/notification-module-architecture.md` mục 8 — dừng poll khi tab ẩn, fetch ngay khi quay lại):

```ts
import { getNotifications } from '@/api/generated/notifications/notifications'

const POLL_INTERVAL_MS = 30_000

export const useNotificationsStore = defineStore('notifications', () => {
  const unreadCount = ref(0)
  const client = getNotifications()
  let intervalHandle: ReturnType<typeof setInterval> | null = null

  async function fetchUnreadCount() {
    const response = await client.notificationsGetUnreadCount()
    unreadCount.value = response.data.count
  }

  function resume() {
    fetchUnreadCount()
    intervalHandle ??= setInterval(fetchUnreadCount, POLL_INTERVAL_MS)
  }

  function pause() {
    if (intervalHandle) clearInterval(intervalHandle)
    intervalHandle = null
  }

  function handleVisibilityChange() {
    if (document.visibilityState === 'visible') resume()
    else pause()
  }

  function startPolling() {
    document.addEventListener('visibilitychange', handleVisibilityChange)
    if (document.visibilityState === 'visible') resume()
  }

  function stopPolling() {
    pause()
    document.removeEventListener('visibilitychange', handleVisibilityChange)
  }

  return { unreadCount, fetchUnreadCount, startPolling, stopPolling }
})
```

Gọi `startPolling()`/`stopPolling()` trong `App.vue` (`onMounted`/`onUnmounted`) — nơi duy nhất sống suốt vòng đời app, tương tự cách `useThemePreference`/`useLocaleStore` đã được gọi ở đó.

### New: `frontend/src/components/NotificationBell.vue`

Icon chuông + `n-badge` hiển thị `unreadCount` (ẩn khi 0) trong `AppHeader.vue`, click mở dropdown/popover gọi `client.notificationsGetAll({ pageSize: 10 })` (lazy load, chỉ fetch khi mở, không phải lúc poll). Mỗi item render theo `Type`:

```ts
const { t } = useI18n()
function renderNotification(n: Notification) {
  const data = n.data ? JSON.parse(n.data) : {}
  return t(`notifications.${n.type}`, data)
}
```

Bấm "đánh dấu đã đọc" gọi `client.notificationsMarkAsRead(id)` rồi `notificationsStore.fetchUnreadCount()`; bấm "đánh dấu tất cả" tương tự với `notificationsMarkAllAsRead()`.

### Edit: `frontend/src/layouts/AppHeader.vue`

Thêm `<NotificationBell />` cạnh các icon hiện có.

### Edit: `frontend/src/locales/{vi,en}.ts`

```ts
// vi.ts
notifications: {
  organizationMemberAdded: 'Bạn đã được thêm vào một tổ chức',
},

// en.ts
notifications: {
  organizationMemberAdded: 'You were added to an organization',
},
```

`organizationId` trong `Data` chưa dùng để hiển thị tên tổ chức ở Phase 1 (xem Part 3) — chỉ dùng làm chỗ neo cho tính năng deep-link nếu làm sau.

---

## Tests

**Backend**:
- `backend/tests/StarterKit.Domain.Tests/Entities/NotificationTests.cs` — `Create` với `AccountId`/`Type` rỗng ném `DomainException`; `MarkRead()` set `ReadAt` đúng 1 lần (gọi 2 lần không đổi lần đầu).
- `backend/tests/StarterKit.Application.Tests/Services/Notifications/NotificationServiceTests.cs` — theo mẫu `Fixture`/`CreateFixture()` của `AuditLogServiceTests`/`SessionServiceTests`: `NotifyAsync` persist đúng; `GetMyNotificationsAsync` chỉ trả về notification của đúng account, tôn trọng `unreadOnly`; `MarkAsReadAsync` ném `NotFoundException` khi không tồn tại **hoặc** thuộc account khác; `MarkAllAsReadAsync` đánh dấu hết unread của account, không đụng account khác.
- `backend/tests/StarterKit.API.Tests/Controllers/NotificationsControllerTests.cs` — end-to-end qua `WebApplicationFactory` (giống `OrganizationsControllerTests`): 401 khi chưa đăng nhập, 200 với danh sách rỗng cho account mới, flow add-member-to-org rồi assert notification xuất hiện cho account được thêm.

**Frontend**:
- `frontend/tests/unit/stores/notifications.test.ts` — mock `msw`, assert `startPolling`/`stopPolling` gắn/gỡ đúng `visibilitychange` listener, `fetchUnreadCount` cập nhật state.
- `frontend/tests/unit/components/NotificationBell.test.ts` — badge ẩn khi `unreadCount === 0`, render đúng text theo `type` qua i18n mock.

---

## Verification

```bash
dotnet build backend/StarterKit.sln --no-restore -m:1
dotnet test backend/StarterKit.sln --no-restore -m:1
bun run --cwd frontend codegen
bun run --cwd frontend build   # type-check
bun run --cwd frontend test:run
```

Thủ công: đăng nhập 2 tài khoản (A là owner org, B chưa vào org nào) → A thêm B vào org qua UI hiện có → đăng nhập bằng B (hoặc đợi poll 30s nếu để tab B luôn mở) → badge chuông tăng lên trong vòng 1 chu kỳ poll.

## Critical Files

- `backend/src/StarterKit.Domain/Entities/Notification.cs` — mới
- `backend/src/StarterKit.Infrastructure/Persistence/Configurations/NotificationConfiguration.cs` — mới
- `backend/src/StarterKit.Infrastructure/Persistence/AppDbContext.cs` — thêm `DbSet<Notification>`
- `backend/src/StarterKit.Infrastructure/Migrations/*_AddNotifications.cs` — sinh bởi `dotnet ef migrations add`
- `backend/src/StarterKit.Application/Services/Notifications/*.cs` — mới (Dto, Types, Interface, Service)
- `backend/src/StarterKit.Application/Services/Organizations/OrganizationMembershipService.cs` — thêm dependency + 1 lệnh gọi trong `AddMemberAsync`
- `backend/src/StarterKit.Application/DependencyInjection.cs` — 1 dòng `AddScoped`
- `backend/src/StarterKit.API/Controllers/NotificationsController.cs` — mới
- `frontend/src/stores/notifications.ts`, `frontend/src/components/NotificationBell.vue` — mới
- `frontend/src/layouts/AppHeader.vue`, `frontend/src/App.vue` — wiring
- `frontend/src/locales/{vi,en}.ts` — thêm key `notifications.organizationMemberAdded`

## Decision log

Sau khi implement xong, sẽ đề xuất 1 entry cho `.claude/decisions.md`: quyết định bỏ Title/Body khỏi entity, để frontend tự render theo `Type`+`Data` — đây là quyết định về data contract (backend↔frontend), thuộc phạm vi cần ghi log theo CLAUDE.md. Sẽ draft và xin xác nhận trước khi ghi, theo đúng gate rule — chưa làm ở bước lập plan này.

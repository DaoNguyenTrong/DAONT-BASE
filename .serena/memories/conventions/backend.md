See top-level `code_style_conventions` memory and `.claude/rules/code-conventions.md` +
`.claude/rules/architecture.md` (always-loaded, authoritative) for backend C# conventions and
Clean Architecture layering. Auth-specific rules: `.claude/rules/authentication.md`
(JWT via Authorization header or access_token cookie; no tenant claims; X-TimeZone header required).

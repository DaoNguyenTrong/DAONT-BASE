See top-level `code_style_conventions` memory and `.claude/rules/frontend-conventions.md`
(always-loaded, authoritative) for Vue/TS conventions, Pinia store style, and the API layer pattern.
Serena's symbol tools (find_symbol, find_referencing_symbols, get_symbols_overview) only cover
`backend/` (C#) today — for `frontend/` (Vue/TS) use grep or the Explore agent, then Read directly;
see `.claude/rules/serena.md`.
